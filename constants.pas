unit constants;

{$mode ObjFPC}{$H+}

interface

uses
  Classes, SysUtils;

const
  screenWidth = 1024;
  screenHeight = 768;

  BLOCK_WIDTH = 50;
  BLOCK_HEIGHT = 25;
  BLOCK_GAP = 15;
  LEFT_MARGIN = 180;
  TOP_MARGIN = 100;
  BLOCK_HORZ_COUNT = 10;
  BLOCK_VERT_COUNT = 9;
  BALL_RADIUS = 10;
  BALL_MARGIN = 600;
  DELTA_LIMIT = 15;
  PADDLE_WIDTH = 60;
  PADDLE_HEIGHT = 20;
  PADDLE_MARGIN = 700;

implementation

end.

